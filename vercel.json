{
  "buildCommand": "cd frontend && npm install && npm run build && cp -r dist ../dist || mv dist ../dist",
  "outputDirectory": "dist",
  "installCommand": "cd frontend && npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
